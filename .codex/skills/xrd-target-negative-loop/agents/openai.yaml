interface:
  display_name: "XRD Target Negative Loop"
  short_description: "Iterate negatives by processed real-data results"
  default_prompt: "Use $xrd-target-negative-loop to choose negatives for one target formula, iterate training with a subagent, and decide completion only from processed real-data results after threshold tuning."
